import 'package:flutter/material.dart';

class IMSearchPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("搜索好友"),
      ),
      body: Container(
        margin: EdgeInsets.all(16),
      ),
    );
  }
}
